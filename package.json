{
  "private": true,
  "name": "monorepo",
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "patch-package": "patch-package --exclude '^$'",
    "postinstall": "patch-package"
  }
}
